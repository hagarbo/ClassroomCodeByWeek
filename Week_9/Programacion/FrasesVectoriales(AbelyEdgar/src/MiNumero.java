public class MiNumero {
    public boolean esVampiro(int numero) {
        String[] numeros = String.valueOf(numero).split("");
        if (numeros.length % 2 != 0)
            return false;
        int colmillosLength = numeros.length / 2;

        return false;
    }
}
